Circular avatar. Shows the photo when `src` is given, else colored initials from `name`.

```jsx
<Avatar name="Maya Chen" />
<Avatar name="Sam Okafor" src="/me.jpg" size="lg" />
```

Sizes `xs | sm | md | lg`. Tint is chosen deterministically from the name.
