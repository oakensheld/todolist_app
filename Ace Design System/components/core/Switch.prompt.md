Boolean toggle for settings. Knob springs across on change; track turns brand blue when on.

```jsx
<Switch checked={remind} onChange={setRemind} label="Daily reminder" />
<Switch checked={v} onChange={setV} size="sm" />
```
