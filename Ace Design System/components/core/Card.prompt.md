The default content container: white, 12px radius, hairline border, soft shadow.

```jsx
<Card>…</Card>
<Card interactive elevation="sm" onClick={open}>…</Card>
```

`interactive` adds a hover lift (translateY -2px + deeper shadow). Tune resting shadow with `elevation`.
